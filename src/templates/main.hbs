{{#each this}}
<div>
    <li>
        <article>
            <img src={{flag}} alt="" width="320">
            <h2>Country: {{name}}</h2>
            <p>Capital: {{capital}}</p>
            <p>Population {{population}}</p>
            <p>Languages:</p>
            <ul>
                {{#each languages}}
                <li>{{name}}</li>
                {{/each}}
            </ul>
        </article>
        </a>
    </li>
</div>
{{/each}}